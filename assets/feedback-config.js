/*
 * WorkTools feedback configuration.
 *
 * Paste your Supabase project values below. Both are safe to commit and expose
 * publicly — the anon key is designed to be public, and Row Level Security is
 * what actually protects the data (the public can only INSERT feedback, never
 * read it). See docs/feedback-setup.md for the one-time setup steps.
 */
window.WORKTOOLS_SUPABASE = {
  url: "https://YOUR-PROJECT.supabase.co",
  anonKey: "YOUR-ANON-PUBLIC-KEY"
};
